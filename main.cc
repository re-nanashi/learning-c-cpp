#include <algorithm>
#include <iostream>
#include <map>
#include <string>
#include <vector>
using namespace std;

float celcius_convert(int f)
{
    float far = (5 / 9.0) * (f - 32);
    return far;
}

int main()
{
    int x = 2;
    int y = 2;
    int result = f(z + y); // (4 + 2) + 1 not

    std::cout << result << std::endl;
    std::cout << z << std::endl;

    return 0;
}
